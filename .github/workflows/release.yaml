name: github-release

on:
  workflow_dispatch:

jobs:
  release:
    uses: wittdennis/pipelines/.github/workflows/cog-release.yaml@0d6d7ea86ee5a77cd1493ef7cae0a749787ce637 # 2.0.41
    with:
      check-latest-tag-only: true
    secrets:
      release-token: ${{ secrets.RELEASE_TOKEN }}
