name: github-release

on:
  workflow_dispatch:

jobs:
  release:
    uses: wittdennis/pipelines/.github/workflows/cog-release.yaml@06645518a72040625d2b681086324d8a266c41c0 # 2.0.47
    with:
      check-latest-tag-only: true
    secrets:
      release-token: ${{ secrets.RELEASE_TOKEN }}
